[project]
name = "zeroone-ops"
version = "0.57.0"
description = "Governed OpenAI-assisted pipeline agents for GitLab and GitHub."
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
  "gitpython>=3.1.59",
  "httpx>=0.28.1",
  "mako>=1.4.1",
  "markdown-it-py>=4.2.0",
  "mlflow-tracing>=3.15.1",
  "openai>=2.52.0",
  "pydantic>=2.13.4",
  "python-dotenv>=1.2.3",
  "typer>=0.27.1",
  "urllib3==2.7.0",
]

[project.scripts]
zeroone-ops = "zeroone_ops.cli:main"
ai-sonar-bot = "zeroone_ops.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zeroone_ops"]

[tool.hatch.build.targets.wheel.package-data]
"zeroone_ops" = ["prompts/*.txt", "py.typed"]

[tool.hatch.build.targets.sdist]
include = [
  "src/zeroone_ops/**/*.py",
  "src/zeroone_ops/prompts/*.txt",
  "src/zeroone_ops/py.typed",
  "README.md",
]

[tool.uv]
default-groups = ["dev"]
exclude-newer = "7 days"

[tool.uv.exclude-newer-package]
# Temporary security overrides; remove once these releases fall within the
# global exclude-newer window.

[dependency-groups]
dev = [
  "bandit>=1.9.4",
  "import-linter>=2.13",
  "pre-commit>=4.6.2",
  "mypy>=2.3.1",
  "pip==26.2.1",
  "pip-audit>=2.10.1",
  "pytest>=9.1.1",
  "pytest-cov>=7.1.0",
  "pyyaml>=6.0.3",
  "ruff>=0.16.3",
]

[tool.ruff]
target-version = "py313"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
  "D",
  "E",
  "F",
  "I",
  "PLC0415",
  "UP",
  "B",
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"samples/**/*.py" = ["E712"]
"samples/ruff_findings/**/*.py" = ["E712", "F841"]
"tests/**/*.py" = ["D"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.13"
strict = true
packages = ["zeroone_ops"]
warn_unused_configs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "--cov=zeroone_ops --cov-report=term-missing --cov-fail-under=80"
